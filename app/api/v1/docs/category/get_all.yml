Endpoint that retrieves all categories to a question
---
tags:
  - Category
responses:
  200:
    description: A list of created categories to a question
    schema:
      type: object
      properties:
        categories:
          type: array
  404:
    description: No category found
    schema:
      type: object
      properties:
        message:
          type: string
          enum:
            - "No category found"
